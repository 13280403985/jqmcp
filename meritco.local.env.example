# 复制为 meritco.local.env（勿提交仓库）。终端与 MCP 共用：MCP 的 env 里只配 MERITCO_CONFIG_DIR。
#
# —— 推荐：持久化「内置 Chromium」登录一次，免抄 Cookie/Token（通用查询 Playwright）——
# 1) 项目目录执行 npm run build && npm run meritco:profile，在弹出窗口登录并进入 bot，回车退出
# 2) 在此文件增加：MERITCO_USE_PERSIST_PROFILE=1
# 3) 重载 MCP。会话写入 meritco-chromium-profile/（勿提交）
# 自定义目录可设 MERITCO_CHROMIUM_USER_DATA=C:\path\to\profile（可不设 USE_PERSIST）
# MERITCO_USE_PERSIST_PROFILE=1 时，通用查询（MCP / query:pw）默认**无头后台**不弹窗；要看浏览器请设 MERITCO_PLAYWRIGHT_HEADLESS=0
#
# 也可用 Playwright 登录后写 Cookie/Token：npm run sync:env（MERITCO_CONVERSATION_ID 可从会话 URL 或 history 接口对应字段填写）。
#
# —— 办公室网关（npm run gateway）：同事 HTTP 调通用查询，与本机 MCP 共用下方久谦凭证 ——
# MERITCO_GATEWAY_API_KEY=请换成长随机串
# MERITCO_GATEWAY_PORT=8787
# MERITCO_GATEWAY_HOST=0.0.0.0
# 可选：仅允许这些客户端 IP（逗号分隔，精确匹配）；不配则仅依赖 API Key
# MERITCO_GATEWAY_ALLOW_IPS=192.168.1.10,192.168.1.11
# 可选：CORS，浏览器直连时设具体源；不配则默认 *
# MERITCO_GATEWAY_CORS_ORIGIN=*
# 已存在于系统环境 / mcp.json 的键不会被覆盖。

# 持久化目录与 meritco-auth.json 并存时：默认**不再**用 auth 覆盖 localStorage（防止旧 token 冲掉 profile）。仅 profile 会话且 auth 很旧时改：
# MERITCO_PERSIST_MERGE_AUTH_LOCALSTORAGE=1

# meritco_history_get（HTTP）需要时再取消注释并填写：
# MERITCO_CONVERSATION_ID=
# MERITCO_TOKEN=
# MERITCO_COOKIE=
# MERITCO_COOKIE_FILE=C:/Users/ASUS/Desktop/JQMCP/meritco-cookie.txt
# Playwright：npx playwright codegen … --save-storage=meritco-auth.json 登录后保存；须含非空 cookies
# MERITCO_STORAGE_STATE=meritco-auth.json
